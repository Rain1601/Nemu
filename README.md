# Nemu

Nemu is a compact desktop working-memory app built with Tauri, React, and TypeScript.

## Development

```sh
npm install
npm run tauri dev
```

## Release

Push a `v*` tag to build the macOS app and attach the generated DMG to a GitHub Release.
